import React from 'react'

import { Header } from '../../../../payload/payload-types'

type Props = {
  header: Header
}

const MobileNav = ({ header }: Props) => {
  return <div>MobileNav</div>
}

export default MobileNav
